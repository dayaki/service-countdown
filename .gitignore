node_modules/
.next/
out/
build/

.DS_Store
*.pem

npm-debug.log*
yarn-debug.log*
yarn-error.log*

.env*
!.env.example

next-env.d.ts
.vercel
*.tsbuildinfo

# Source videos are 672MB total and four exceed GitHub's 100MB per-file limit,
# so they are kept out of git. Track them with Git LFS if they need versioning.
public/videos/
