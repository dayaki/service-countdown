node_modules/
.next/
out/
build/

.DS_Store
*.pem

npm-debug.log*
yarn-debug.log*
yarn-error.log*

.env*
!.env.example

next-env.d.ts
.vercel
*.tsbuildinfo

# Service media is swapped out for every service, so it lives outside git.
# The app reads whatever is in these folders at runtime, no code change needed.
# Videos are also 672MB total, four of them over GitHub's 100MB file limit.
public/videos/
public/images/

# Past services' clips, kept on disk to swap back in. 3.2GB, and thirteen of
# them are over GitHub's 100MB limit on their own.
public/video_bks/
